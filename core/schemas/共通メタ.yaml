# 共通メタ情報スキーマ
# すべての usecase の出力 YAML が末尾に含めるべきメタ情報。
#
# 注: YAML には実装的な include 機構がない。各 usecase の template.yaml で
# 同じ項目を末尾に書き足す形で運用する。このファイルは "正規の定義" として参照する。

作成日: ""        # YYYY-MM-DD
最終更新: ""      # YYYY-MM-DD
バージョン: "1"   # スキーマの版
