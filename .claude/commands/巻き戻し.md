---
description: 過去の状態に戻す
---

1. `/履歴` を呼んで番号付きで表示
2. ユーザーに番号を選んでもらう
3. 戻すと「何が消えるか」を先に説明
4. ユーザーが yes と答えたら：
   - デフォルトは `git revert <hash>`（履歴を残す＝安全）
   - 「履歴ごと消したい」と明示されたら `git reset --hard <hash>`
5. 結果を報告

実行前に必ず確認を取ること。失われる作業がないか丁寧に確認する。
